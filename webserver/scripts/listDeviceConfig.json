{
    "name": "List Device Config History",
    "description": "List config files for specified device",
    "commands": [
        {
            "command": "cd config/${device} && ls -lthGr",
            "description": "Go into the device directory.  Paste in the 19 digit hardware number.",
            "parameters": {
                "device": "19 digit hardware number"
            }
        }
    ]
}